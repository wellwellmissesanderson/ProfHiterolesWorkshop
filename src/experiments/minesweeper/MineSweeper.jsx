import React from 'react';

const MineSweeper = ({ children }) => {
    return (
        <div>
            IM MINESWEEPER
            <br/>
            {children}
        </div>
    );

}

export default MineSweeper;

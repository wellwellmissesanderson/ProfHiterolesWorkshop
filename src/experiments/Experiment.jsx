import React from 'react';

const Experiment = (props) => {
    return (
        <div>
            IM AN EXPERIMENT!!
            <br/>
            {props.children}
        </div>

    );

}

export default Experiment;
